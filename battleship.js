var board = [];for (i = 0; i < board.length; i++) {                                                        //not sure    board.push(["O"] * 5);}function print_board(board) {    console.log("* * * * *");    for (i = 0; i < board.length; i++) {        console.log( " ").join(row)  ;                                                      //not sure    console.log("* * * * *");    return}console.log("Let's play Battleship!")print_board(board);                                                                         //not surefunction random_row(board) {    return Math.floor((Math.random() * 5) + 1);}function random_col(board) {    return Math.floor((Math.random() * 5) + 1);}var ship_row = random_row(board);var ship_col = random_col(board);for (i = 0; i < board.length; i++) {                                                        //not sure    board.push(["O"] * 5);    console.log("Turn", turn + 1);    var guess_row = prompt("Guess Row:");    var guess_col = prompt("Guess Col:");        if guess_row == "" || guess_col == "" {        console.log("Not a valid guess.");        print_board(board);                                                                 //not sure        continue                                                } else {        var guess_row = parseInt(guess_row);        var guess_col = parseInt(guess_col);    }    if guess_row == ship_row && guess_col == ship_col {         console.log("Congratulations! You sunk my battleship!");        break    } else {        if (guess_row < 0 || guess_row > 4) || (guess_col < 0 || guess_col > 4) }          //not sure            console.log("Oops, that's not even in the ocean.");        } else if (board[guess_row][guess_col] == "X") {            console.log("You guessed that one already.")        } else if guess_row == ship_row || guess_col == ship_col {            if var guess_col == var ship_col {                console.log("You got the row correct.");            }            if guess_col == ship_col {                console.log("you got the column right.");            }            board[guess_row][guess_col] = "X";                                               //not sure            print_board(board);        } else {            console.log("You missed my battleship!")            board[guess_row][guess_col] = "X";                                              //not sure            print_board(board);        }        if var turn == 3 {            console.log("Game Over");            console.log("The correct row was " + str(ship_row) + " and the correct column was " + str(ship_col)        }    }}    